{
  "name": "guessit-analytics",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create guessit-analytics",
    "db:migrate": "wrangler d1 execute guessit-analytics --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute guessit-analytics --local --file=schema.sql",
    "db:query": "wrangler d1 execute guessit-analytics --command",
    "db:query:local": "wrangler d1 execute guessit-analytics --local --command"
  }
}
